[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "e2e: end-to-end tests requiring a running server",
]

[tool.coverage.run]
source = ["part_lister"]

[tool.coverage.report]
show_missing = true
